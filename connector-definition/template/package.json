{
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "ts-node index.ts serve --configuration=.",
    "watch": "nodemon --watch \"./**\" --ext \"ts,json\" --exec \"ts-node index.ts serve --configuration=."
  },
  "dependencies": {
    "@hasura/ndc-duckduckapi": "*",
    "ts-node": "^10.9.2",
    "@tsconfig/node20": "^20.1.4"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.2"
  }
}
